버블 정렬을 사용하는 프로그램을 이용해 배열을 오름차순으로 정렬하려고 한다.
조건1 main()에서 1~50 사이의 숫자를 랜덤하게 만들어 배열에 저장한다.
조건2 배열을 매개변수로 받는 메쏘드를 생성하여 동작 처리하려고 한다.

1번을 위해 일단 메인에 랜덤 숫자를 받을 수 있는 랜덤 클래스와 정수형을 받을 수 있는 arr이라는 배열의 크기를 50으로 설정한다.
 static void Main(string[] args)
        {
            Random r = new Random();

            int[] arr = new int[50];
            
그리고 반복문을 통해 배열 arr의 크기까지 랜덤한 숫자를 넣어준다.
for (int i = 0; i < arr.Length; i++)
            {
                int a = r.Next(1, 50);
                arr[i] += a
            }
그렇게 하면 조건 1번의 조건을 충족하게 된디. 조건 2번 조건을 충족하기 위해 

새로운 매서드를 생성한다. 
public static void Bubble(int[] arr)

그리고 버블 정렬은 배열을 비교하여 정렬하는것이기에 배열안에 있는 정수들을 비교하는 코드를 작성한다
 for (int i = 0; i < arr.Length; i++)
            {
                for (int j = i + 1; j < arr.Length; j++)
                {
                
                }
                
            }
            
